<div class="rolang-collection-template">
  <div class="rolang-collection-hero">
    <p class="rolang-eyebrow">{{ collection.all_products_count }} products</p>
    <h1>{{ collection.title }}</h1>
    {% if collection.description != blank %}
      <div class="rolang-collection-copy">{{ collection.description }}</div>
    {% else %}
      <p class="rolang-collection-copy">
        Use Shopify collections to group products by brand, skin type, or routine without rebuilding the storefront.
      </p>
    {% endif %}
  </div>

  {% paginate collection.products by section.settings.products_per_page %}
    {% assign current_sort = collection.sort_by | default: collection.default_sort_by %}

    <div class="rolang-product-form-card">
      <div class="rolang-product-form-fields">
        <div>
          <p class="rolang-eyebrow">Collection tools</p>
          <p>
            {% if paginate.items == 1 %}
              1 product available in this collection.
            {% else %}
              {{ paginate.items }} products available in this collection.
            {% endif %}
          </p>
        </div>

        <form action="{{ request.path }}" method="get">
          <div class="rolang-field-group">
            <label for="SortBy-{{ section.id }}">Sort products</label>
            <select id="SortBy-{{ section.id }}" name="sort_by">
              {% for option in collection.sort_options %}
                <option
                  value="{{ option.value }}"
                  {% if option.value == current_sort %}
                    selected="selected"
                  {% endif %}
                >
                  {{ option.name }}
                </option>
              {% endfor %}
            </select>
          </div>

          <div class="rolang-cart-actions">
            <button class="rolang-button-secondary" type="submit">Apply sort</button>
            {% if collection.handle != 'all' %}
              <a class="rolang-inline-link" href="{{ routes.all_products_collection_url }}">Browse all products</a>
            {% endif %}
          </div>
        </form>
      </div>
    </div>

    {% if paginate.items == 0 %}
      <div class="rolang-product-form-card">
        <div class="rolang-product-form-fields">
          <div>
            <p class="rolang-eyebrow">Nothing here yet</p>
            <p>This collection does not have any products yet. Browse the full catalog while it is being updated.</p>
          </div>

          <div class="rolang-cart-actions">
            <a class="rolang-button" href="{{ routes.all_products_collection_url }}">Browse all products</a>
          </div>
        </div>
      </div>
    {% else %}
      <div class="rolang-product-grid">
        {% for product in collection.products %}
          <article class="rolang-product-card">
            <div class="rolang-product-card-image">
              {% if product.featured_image %}
                {% render 'image',
                  image: product.featured_image,
                  url: product.url,
                  width: 640,
                  height: 640,
                  crop: 'center'
                %}
              {% endif %}
            </div>
            <div class="rolang-product-card-copy">
              <span class="rolang-product-meta">{{ product.vendor | default: shop.name }}</span>
              <strong>{{ product.title }}</strong>
              <div class="rolang-price-row">
                <span class="rolang-price">
                  {% if product.price_varies %}
                    From {{ product.price_min | money }}
                  {% else %}
                    {{ product.price | money }}
                  {% endif %}
                </span>
                {% if product.compare_at_price > product.price %}
                  <span class="rolang-compare-price">{{ product.compare_at_price | money }}</span>
                {% endif %}
              </div>
              <div class="rolang-detail-row">
                <span class="rolang-detail-pill">
                  {% if product.available %}
                    Available for checkout
                  {% else %}
                    Sold out
                  {% endif %}
                </span>
                {% if product.price_varies %}
                  <span class="rolang-detail-pill">{{ product.variants.size }} options</span>
                {% endif %}
              </div>
              <div class="rolang-tag-row">
                {% for tag in product.tags limit: 3 %}
                  <span class="rolang-chip">{{ tag }}</span>
                {% endfor %}
              </div>
              <a class="rolang-inline-link" href="{{ product.url }}">View product</a>
            </div>
          </article>
        {% endfor %}
      </div>
    {% endif %}

    {{ paginate | default_pagination }}
  {% endpaginate %}
</div>

{% schema %}
{
  "name": "Collection",
  "settings": [
    {
      "type": "range",
      "id": "products_per_page",
      "label": "Products per page",
      "min": 8,
      "max": 36,
      "step": 4,
      "default": 16
    }
  ]
}
{% endschema %}
